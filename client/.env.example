# Client environment variables example
# When building the client separately (not served by the backend), set the backend URL here.
# Include protocol (https://) and optional port.
REACT_APP_BACKEND_URL=
