# Server environment variables example
# Render sets PORT automatically; override only for local testing
PORT=8000

# If you want to restrict CORS to your frontend origin in production, set:
# FRONTEND_ORIGIN=https://your-frontend-domain.com
FRONTEND_ORIGIN=

NODE_ENV=production
